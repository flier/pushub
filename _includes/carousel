<link href="{{ ASSET_PATH }}/css/carousel.css" rel="stylesheet" media="screen">

<div id="{{ page.carousel.id }}" class="carousel slide">
  <div class="carousel-inner">
    {% for item in page.carousel.items %}
      <div class="item">
        <img src="{{ ASSET_PATH }}/img/{{ item.img }}" alt="{{ item.title }}">
        <div class="container">
          <div class="carousel-caption">
            <h1>{{ item.title }}</h1>
            <p class="lead">{{ item.desc }}</p>
            <a class="btn btn-large btn-primary" href="{{ item.action.url }}">{{ item.action.name }}</a>
          </div>
        </div>
      </div>
    {% endfor %}
  </div>
  <a class="left carousel-control" href="#{{ page.carousel.id }}" data-slide="prev">‹</a>
  <a class="right carousel-control" href="#{{ page.carousel.id }}" data-slide="next">›</a>
</div>